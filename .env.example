# Copy to .env and fill in. Optional — without it, the challenge generator uses
# built-in fallback challenges so the demo still works.
GEMINI_API_KEY=

# Optional: override the port (Cloud Run sets PORT automatically).
# PORT=8080
